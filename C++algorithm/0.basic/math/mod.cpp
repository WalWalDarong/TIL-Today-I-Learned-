#include <bits/stdc++.h>
using namespace std;

int main(){
  //모듈러 연산
  /*
  나머지의 원칙
  0. a == b % n 이고, b == c % n 이면 a = c % n 이다.
  1. [(a % n) + (b % n)] % n = (a + b) % n
  2. [(a % n) - (b % n)] % n = (a - b) % n
  3. [(a % n) * (b % n)] % n = (a * b) % n
  */

  return 0;
}